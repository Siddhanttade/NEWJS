//while comparing 2 diff entities it is necessary to keep their datatype same

console.log(null>0);
console.log(null==0);
console.log (null>=0);

console.log(undefined>0);

//when strict checking is done using === it automatically converts the dattatype and checks whether the comparison is possible or not 